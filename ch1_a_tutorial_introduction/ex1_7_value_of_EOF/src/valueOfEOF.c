#include <stdio.h>

/**
 * @brief Prints out the value of EOF
 *  
 * @param [out] result Indicates if the program executed successfully or not
*/
int main()
{
    printf("The value of EOF is: %d\n", EOF);
    return 0;
}
